# Gradle / Loom
.gradle/
build/
remappedSrc/
.fabric/
run/

# Stonecutter (generated per-version build output and caches; versions/<v>/gradle.properties is tracked)
versions/*/build/
.stonecutter/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project
bin/

# Claude Code / local agent tooling (per-developer, may contain local paths)
.claude/

# OS
.DS_Store
Thumbs.db

# Node (mcp-server)
node_modules/
dist/
*.log

# mcpfabric runtime config (contains auth token)
mcpfabric.config.json
